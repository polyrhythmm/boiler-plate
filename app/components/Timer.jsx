var React = require('react');

var Timer = React.createClass({

  render: function() {
    return (
      <div>
        <h1>Timer</h1>
        <p>This is the timer page</p>
      </div>
    );
  }
});

module.exports = Timer;
